import java.util.*;

public class BorrowRecordList {
    List<BorrowRecord> records;
    List<BorrowRecord> activeRecords;
    List<BorrowRecord> returnRecords;

    public BorrowRecordList() {
        records = new ArrayList<>();
        activeRecords = new ArrayList<>();
        returnRecords = new ArrayList<>();
    }

    public void addRecord(BorrowRecord b) {

    }

}
